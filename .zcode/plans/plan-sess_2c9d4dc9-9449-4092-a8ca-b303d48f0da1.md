# ZenuxsDesign Theme + Auth + Loading + Overview Redesign Plan

## Phase 1: Theme System Update

### 1.1 Update `src/app.css` color tokens
- Dark: `#111315` bg, `#17191C` panel, `#1E2126` surface, `#292D33` border, `#F5F7FA` text, `#A1A7B0` secondary, `#737A85` muted, `#3B82F6` accent
- Light: `#F7F8FA` bg, `#FFFFFF` panel, `#F1F3F5` secondary, `#D9DEE5` border, `#17191C` text, `#5F6670` secondary, `#8A919B` muted, `#2563EB` accent
- All semantic tokens work in both themes

### 1.2 Fix theme toggle in `src/components/TopBar.vue`
- Import `useAppTheme` from `@/app/shell/theme`
- Wire `toggleTheme()` to sun/moon button click
- Toggle icon based on current theme

### 1.3 Theme persistence already works via localStorage

## Phase 2: Auth Flow

### 2.1 Initialize auth at app startup in `src/App.vue`
- Call `initAuth()` on mount

### 2.2 Add route guard in `src/router.ts`
- Protected routes: `/editor`, `/storage`, `/demo`, `/share/:roomId`
- Unauthenticated → redirect to `/`
- Landing page always public
- Allow through after OAuth callback

### 2.3 Update `src/views/WorkspaceView.vue`
- Auth redirect on mount if not authenticated

## Phase 3: Loading Screen Redesign

### 3.1 Replace `index.html` splash screen
- ZenuxsDesign "Z" logo with pulse animation
- "ZenuxsDesign" text + "Loading..." below
- Support dark/light themes via `data-theme`
- CSS-only, no JS

## Phase 4: Landing Page Auth Integration

### 4.1 Update `src/components/LandingPage.vue`
- Use theme tokens instead of hardcoded colors
- Get Started → existing OAuth flow
- Explore → /editor direct

## Phase 5: AI Panel Preservation

### 5.1 Verify AI panel intact
- Only apply theme tokens, no functional changes
- All existing controls preserved

## Files to Modify
1. `src/app.css` - Theme tokens
2. `src/components/TopBar.vue` - Wire theme toggle
3. `src/App.vue` - Initialize auth at startup
4. `src/router.ts` - Auth route guard
5. `src/views/WorkspaceView.vue` - Auth redirect
6. `index.html` - Loading screen
7. `src/components/LandingPage.vue` - Theme tokens